services:
  redis-cache:
    image: redis:alpine
    container_name: rate-limiter-redis
    ports:
      - "6380:6379"
  api-server:
    build: .  
    container_name: rate-limiter-api
    ports:
      - "3000:3000"
    depends_on:
      - redis-cache 